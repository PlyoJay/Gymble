<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell ="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
                    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:converter="clr-namespace:Gymble.Converter">

    <!-- Converter Classes -->
    <converter:RoundRectClipConverter x:Key="RoundRectClipConverter"/>
    <converter:TopRoundRectClipConverter x:Key="TopRoundRectClipConverter"/>
    
    <!-- 폰트 -->
    <FontFamily x:Key="Pretendard.ExtraLight">pack://application:,,,/Resources/Fonts/#Pretendard ExtraLight</FontFamily>
    <FontFamily x:Key="Pretendard.Light">pack://application:,,,/Resources/Fonts/#Pretendard Light</FontFamily>
    <FontFamily x:Key="Pretendard.Thin">pack://application:,,,/Resources/Fonts/#Pretendard Thin</FontFamily>
    <FontFamily x:Key="Pretendard.Regular">pack://application:,,,/Resources/Fonts/#Pretendard</FontFamily>
    <FontFamily x:Key="Pretendard.Medium">pack://application:,,,/Resources/Fonts/#Pretendard Medium</FontFamily>
    <FontFamily x:Key="Pretendard.SemiBold">pack://application:,,,/Resources/Fonts/#Pretendard SemiBold</FontFamily>
    <FontFamily x:Key="Pretendard.ExtraBold">pack://application:,,,/Resources/Fonts/#Pretendard ExtraBold</FontFamily>
    <FontFamily x:Key="Pretendard.Black">pack://application:,,,/Resources/Fonts/#Pretendard Black</FontFamily>

    <!-- SolidColorBrush -->
    <SolidColorBrush x:Key="ApplicationBgColor" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ApplicationBorderColor" Color="#D0D0D0"/>

    <SolidColorBrush x:Key="TitleBarBgColor" Color="#DFDFDF"/>

    <SolidColorBrush x:Key="DefaultBorderBrushColor" Color="#DFDFDF"/>
    <SolidColorBrush x:Key="SeperatorBorderBrushColor" Color="#999999"/>

    <SolidColorBrush x:Key="SearchTextBoxBgColor" Color="#F2F2F2"/>
    <SolidColorBrush x:Key="SearchTextBoxGuideForeground" Color="#989898"/>

    <SolidColorBrush x:Key="NoDataTextBlockForeground" Color="#A9A9A9"/>

    <SolidColorBrush x:Key="BaseButtonBgColor" Color="#2E66E1"/>
    <SolidColorBrush x:Key="BaseButtonBorderBrushColor" Color="#2E66E1"/>
    <SolidColorBrush x:Key="BorderedButtonBgColor" Color="#B2DCFF"/>
    <SolidColorBrush x:Key="BorderedButtonBorderBrushColor" Color="#94B6FF"/>

    <SolidColorBrush x:Key="ReadMoreFgColor" Color="#2E66E1"/>

    <SolidColorBrush x:Key="TabSelectedBrush" Color="#2E66E1"/>
    <SolidColorBrush x:Key="TabUnselectedBrush" Color="#F6F7F7"/>
    <SolidColorBrush x:Key="TabSelectedFg" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TabUnselectedFg" Color="#000000"/>
    <SolidColorBrush x:Key="TabBorderBrush" Color="#DFDFDF"/>
    <SolidColorBrush x:Key="TabContentBrush" Color="Transparent"/>

    <!-- 윈도우 -->
    <Style x:Key="WindowChromeStyle" TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource ApplicationBgColor}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ApplicationBorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome CaptionHeight="35"
                                    CornerRadius="20"
                                    GlassFrameThickness="0"
                                    NonClientFrameEdges="None"
                                    ResizeBorderThickness="0"
                                    UseAeroCaptionButtons="True"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 윈도우 상단 버튼 -->
    <Style x:Key="WIndowChromeButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#00000000"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#00000000"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="30"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E8582B"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D58C3A"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- TextBlock -->
    <Style x:Key="TitleTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.Regular}"/>
    </Style>

    <Style x:Key="SearchTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="10 0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource SearchTextBoxGuideForeground}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.Regular}"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
    </Style>

    <Style x:Key="InfoTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.Regular}"/>
    </Style>

    <Style x:Key="NoDataTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.Medium}"/>
        <Setter Property="Foreground" Value="{StaticResource NoDataTextBlockForeground}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="ContentDataTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.Medium}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="AddMemberWindowTextBlockStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    
    

    <!-- TextBox -->
    <Style x:Key="SearchTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="{StaticResource SearchTextBoxBgColor}"/>
        <Setter Property="Padding" Value="10 0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Foreground" Value="{StaticResource SearchTextBoxGuideForeground}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.Regular}"/>
        <Setter Property="md:TextFieldAssist.CharacterCounterVisibility" Value="Collapsed"/>
    </Style>
    
    <Style x:Key="AddMemberWindowTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#DEDEDE"/>
        <Setter Property="Padding" Value="10 0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="37"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="md:TextFieldAssist.CharacterCounterVisibility" Value="Collapsed"/>
    </Style>

    <!-- 버튼 -->
    <Style x:Key="BaseButtonStyle" TargetType="{x:Type Button}"
       BasedOn="{StaticResource MaterialDesignRaisedDarkButton}">
        <Setter Property="Background" Value="{StaticResource BaseButtonBgColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="#DEDEDE"/>
        <Setter Property="Width" Value="75"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.SemiBold}"/>
        <Setter Property="FontSize" Value="18"/>
    </Style>

    <Style x:Key="BorderedButtonStyle" TargetType="{x:Type Button}"
       BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Background" Value="{StaticResource BorderedButtonBgColor}"/>
        <Setter Property="Foreground" Value="#000000"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderedButtonBorderBrushColor}"/>
    </Style>

    <Style x:Key="ReadMoreButtonStyle" TargetType="{x:Type Button}"
       BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Foreground" Value="{StaticResource ReadMoreFgColor}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="3"/>
    </Style>

    <Style x:Key="AddMemberWindowButton" TargetType="Button">
        <Setter Property="Background" Value="#2E66E1"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="#DEDEDE"/>
        <Setter Property="Width" Value="92"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="4">
                        <ContentPresenter
                        TextElement.Foreground="{TemplateBinding Foreground}"
                        TextElement.FontSize="18"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabControl -->

    <Style x:Key="GymbleTabItemStyle" TargetType="{x:Type TabItem}">
        <Setter Property="Height" Value="40"/>
        <Setter Property="MinWidth" Value="220"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="{StaticResource Pretendard.SemiBold}"/>
        <Setter Property="Foreground" Value="{StaticResource TabUnselectedFg}"/>
        <Setter Property="Background" Value="{StaticResource TabUnselectedBrush}"/>
        <Setter Property="BorderThickness" Value="1 0 0 0"/>
        <Setter Property="BorderBrush" Value="{StaticResource DefaultBorderBrushColor}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ContentPresenter VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          ContentSource="Header"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter TargetName="Bd" Property="BorderThickness" Value="0"/>
                        </Trigger>
                        
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource TabSelectedBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource TabSelectedFg}"/>
                            <Setter Property="Panel.ZIndex" Value="10"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GymbleTabControlStyle" TargetType="{x:Type TabControl}">
        <Setter Property="TabStripPlacement" Value="Top"/>
        <Setter Property="BorderBrush" Value="#D7D7D7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="AlternationCount" Value="1000000"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- ✅ 헤더 스트립 외곽선(Top/Left/Right)을 여기서 한 번만 -->
                        <Border x:Name="bdrHeader" Grid.Row="0"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1,1,1,0"
                                CornerRadius="8,8,0,0"
                                Background="Transparent"
                                SnapsToDevicePixels="True">
                            <Grid x:Name="headerClipHost" SnapsToDevicePixels="True">
                                <Grid.Clip>
                                    <MultiBinding Converter="{StaticResource TopRoundRectClipConverter}"
                                                  ConverterParameter="8">
                                        <Binding RelativeSource="{RelativeSource Self}" Path="ActualWidth"/>
                                        <Binding RelativeSource="{RelativeSource Self}" Path="ActualHeight"/>
                                    </MultiBinding>
                                </Grid.Clip>
                                <TabPanel IsItemsHost="True"/>
                            </Grid>
                        </Border>

                        <!-- 컨텐츠 영역 -->
                        <Border Grid.Row="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0,0,8,8"
                                SnapsToDevicePixels="True">
                                <ContentPresenter ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox -->
    <Style x:Key="ComboDropToggle" TargetType="ToggleButton">
        <!-- 암묵 스타일 영향 제거 -->
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="28"/>
                        </Grid.ColumnDefinitions>
                        <md:PackIcon x:Name="Icon"
                                     Grid.Column="1"
                                     Kind="ChevronDown"
                                     Width="18" Height="18"
                                     Opacity="0.82"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     RenderTransformOrigin="0.5,0.5">
                            <md:PackIcon.RenderTransform>
                                <RotateTransform Angle="0"/>
                            </md:PackIcon.RenderTransform>
                        </md:PackIcon>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Icon" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <!-- 열렸을 때 살짝 위로 뒤집는 느낌 -->
                            <Setter TargetName="Icon" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Icon" Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="ComboPopupBackground" Color="White"/>

    <Style x:Key="MdOutlined.PopupBg"
           TargetType="ComboBox"
           BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid SnapsToDevicePixels="True">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="28"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" 
                                                  Margin="10 0 6 0"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  IsHitTestVisible="False"/>
                                
                                <ToggleButton x:Name="ToggleButton"
                                              Grid.ColumnSpan="2"
                                              Grid.ZIndex="1"
                                              Style="{StaticResource ComboDropToggle}"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              ClickMode="Press"/>
                            </Grid>
                        </Border>

                        <!-- 드롭다운 팝업 -->
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               PlacementTarget="{Binding ElementName=Border}"
                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide"
                               MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                            <Border x:Name="PopupBorder"
                                    Background="{StaticResource ComboPopupBackground}" 
                                    BorderThickness="1" BorderBrush="Gray"
                                    CornerRadius="6"
                                    Padding="4"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer CanContentScroll="True"
                                              HorizontalScrollBarVisibility="Disabled"
                                              VerticalScrollBarVisibility="Auto"
                                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>

                    <!-- 트리거들(필수 ones) -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="PopupBorder" Property="MinHeight" Value="36"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.56"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ▼ 기본 배경 / 텍스트 -->
    <SolidColorBrush x:Key="LineComboBg" Color="#FFFFFF"/>
    <!-- 전체 ComboBox 배경 -->
    <SolidColorBrush x:Key="LineComboBorder" Color="#DEDEDE"/>
    <!-- 전체 ComboBox 배경 -->
    <SolidColorBrush x:Key="LineComboText" Color="#000000"/>
    <!-- 선택된 텍스트 색 -->

    <!-- ▼ 토글 버튼 (드롭다운 버튼) -->
    <SolidColorBrush x:Key="LineComboHover" Color="#767676"/>
    <!-- 드롭다운 버튼 Hover 배경 -->
    <SolidColorBrush x:Key="LineComboArrow" Color="#3F3F3F"/>
    <!-- 드롭다운 화살표 Stroke -->

    <!-- ▼ 아이템 배경 -->
    <SolidColorBrush x:Key="LineComboItemHover" Color="#999"/>
    <!-- 아이템 hover -->
    <SolidColorBrush x:Key="LineComboItemSelected" Color="#767676"/>
    <!-- 아이템 선택 -->

    <Style x:Key="ComboDropToggleStyle" TargetType="ToggleButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- 마우스 올렸을 때 살짝만 진하게, 싫으면 이 트리거 삭제 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity"
                                    Value="0.8" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LineComboBoxItemStyle" TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource LineComboText}" />
        <Setter Property="Background" Value="{StaticResource LineComboBg}" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0 0 0 1"
                            BorderBrush="#264A60"
                            Height="34"
                            Width="{Binding ActualWidth,
                                            RelativeSource={RelativeSource TemplatedParent}}">
                        <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                Value="{StaticResource LineComboItemHover}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Opacity"
                                Value="0.6" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="{StaticResource LineComboBg}"/>
        <Setter Property="Foreground" Value="{StaticResource LineComboText}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="10 0" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource LineComboBoxItemStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Border BorderBrush="{StaticResource LineComboBorder}" BorderThickness="1" 
                            Background="{TemplateBinding Background}" CornerRadius="4">
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition Width="32" />
                            </Grid.ColumnDefinitions>

                            <ContentPresenter x:Name="ContentSite"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              RecognizesAccessKey="True"/>

                            <ToggleButton x:Name="DropDownToggle"
                                      Grid.Column="1"
                                      Style="{StaticResource ComboDropToggleStyle}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                          RelativeSource={RelativeSource TemplatedParent}}">
                                <Path Stroke="{StaticResource LineComboArrow}"
                                      StrokeThickness="1.6"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      Data="M 3 4 L 8 9 L 13 4" />
                            </ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                                <Border Background="{StaticResource LineComboBg}"
                                        BorderThickness="1" BorderBrush="{StaticResource LineComboBorder}"
                                        Width="{Binding ActualWidth,
                                                    RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="350" CornerRadius="4">
                                    <!-- 🔴 여기: 흰 배경 안 나오게 -->
                                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ControlTemplate -->
    
    <ControlTemplate x:Key="StatusControlTemplate" TargetType="{x:Type ContentControl}">
        <Border Background="{TemplateBinding Background}"
                CornerRadius="12">
            <TextBlock
                Text="{TemplateBinding Content}"
                Foreground="{TemplateBinding Foreground}"
                FontSize="16"
                FontFamily="{StaticResource Pretendard.SemiBold}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"/>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="DefaultCardTemplate" TargetType="{x:Type ContentControl}">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{StaticResource DefaultBorderBrushColor}" 
                BorderThickness="1"
                CornerRadius="12">
            <ContentPresenter 
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                Margin="{TemplateBinding Padding}"/>
        </Border>
    </ControlTemplate>

    <!-- DataTemplate -->
    
    <DataTemplate x:Key="SideBarListBoxItemTemplate">
        <StackPanel Orientation="Horizontal" Height="40" Margin="15 0">
            <md:PackIcon Kind="{Binding IconKind}" 
                                 Width="30" Height="30" 
                                 VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Label}" 
                       FontFamily="{StaticResource Pretendard.Regular}"
                       FontSize="20" FontWeight="Bold" 
                       Margin="15 0"
                       VerticalAlignment="Center"/>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>